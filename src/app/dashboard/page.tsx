"use client";

import React from "react";
import { DashboardLayout } from "../../screens/dashboard/DashboardLayout";

/**
 * Dashboard Page
 * 
 * Replaces the monolithic dashboard with a modular, highly performant layout
 * split into subcomponents and sub-sections, optimizing readability, UX, and performance.
 */
export default function Dashboard() {
  return <DashboardLayout />;
}
